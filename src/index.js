//PRIO -
//Styling work on header

// Can I use less data?

// push to github

// Radio button selection must be better
// Work over having to use the QuestioNSelector components inbetween
// quizz app and Question, preferably not

// Make it look decent on mobil

// clean code, most of all styles.css

// Work on Google Maps API feature, come up with how Im gonna use the map

import React from 'react';
import ReactDOM from 'react-dom';
import QuizApp from './components/QuizApp'
import registerServiceWorker from './registerServiceWorker';

ReactDOM.render(<QuizApp />, document.getElementById('root'));
registerServiceWorker();
